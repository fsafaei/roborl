"""Algorithm implementations.

Intentionally empty: algorithms (DQN, PPO, SAC, TD3, ...) arrive in later
sessions, each in its own package with a single readable training loop. See
``docs/algorithm-lifecycle.md`` for the definition of done they must follow.
"""
